{{!< default}}

<main class="gh-main">

    <section class="gh-outer">
        <div class="gh-inner gh-canvas">
            {{#tag}}<h1 class="is-title gh-header-text-tall-upper">{{name}}</h1>{{/tag}}
        </div>
    </section>

    <section class="gh-related-posts gh-outer" aria-label="Posts">
        <div class="gh-inner">

            <ul class="gh-related-posts-feed" role="list">
                {{#foreach posts}}
                <li>
                    <article class="gh-related-post-item" aria-labelledby="gh-tag-post-title-{{@index}}">

                        {{#if feature_image}}
                            <figure class="gh-post-card-group-figure">
                                <img
                                    class="gh-post-card-group-image"
                                    srcset="{{img_url feature_image size="xs" format="webp"}} 160w,
                                            {{img_url feature_image size="s" format="webp"}} 320w,
                                            {{img_url feature_image size="m" format="webp"}} 600w,
                                            {{img_url feature_image size="l" format="webp"}} 960w"
                                    sizes="(max-width: 767px) 100vw, 25vw"
                                    src="{{img_url feature_image size="m"}}"
                                    alt="{{#if feature_image_alt}}{{feature_image_alt}}{{else}}{{title}}{{/if}}"
                                    loading="lazy"
                                    width="600"
                                    height="400"
                                >
                            </figure>
                        {{/if}}

                        <h2 class="is-title gh-header-text-tall-upper gh-header-text-tag-list-item" id="gh-tag-post-title-{{@index}}">
                            <a href="{{url}}">{{title}}</a>
                        </h2>

                        <time class="gh-text-post-attributes" datetime="{{date format="M/DD/YYYY"}}">
                            {{date format="M/DD/YYYY"}}
                        </time>

                        {{#if custom_excerpt}}
                            <p class="is-body">{{custom_excerpt}}</p>
                        {{else}}
                            {{#if excerpt}}
                                <p class="is-body">{{excerpt}}</p>
                            {{/if}}
                        {{/if}}

                    </article>
                </li>
                {{/foreach}}
            </ul>

            {{#match pagination.pages ">" 1}}
                {{pagination}}
            {{/match}}

        </div>
    </section>

</main>
